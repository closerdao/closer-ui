{
  "functions": {
    "pages/api/parse-receipt.ts": {
      "maxDuration": 30
    }
  },
  "build": {
    "env": {
      "SHARP_IGNORE_GLOBAL_LIBVIPS": "1"
    }
  },
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "cd ../.. && turbo run build:locales build:config --filter=closer && turbo run build --filter=tdf"
}
